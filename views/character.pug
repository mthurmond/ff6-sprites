extends layout

block content
  .container
    .d-flex.flex-column.align-items-center.justify-content-center
      .mt-5
      img.mt-5.character-image-general(src=image alt=name)
      .fs-2.pt-2= name
      p.lead.fs-4= intro
      if audio
        audio(controls)
          source(src=audio type="audio/mpeg")
      p.fs-5.pt-4(style='max-width: 32em; text-align: justify')= details