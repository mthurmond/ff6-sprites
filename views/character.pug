extends layout

block content
  .container
    .d-flex.flex-column.align-items-center.justify-content-center
      .fs-2.pt-5= name
      p.lead.fs-4= intro
      img.w-25.shadow.rounded(src=image alt=name)
      p.fs-5.pt-5(style='max-width: 32em; text-align: justify')= details